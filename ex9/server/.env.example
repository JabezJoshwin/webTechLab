# Example environment variables for the server (Render)
MONGO_URI=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/microlearn2
JWT_SECRET=replace_with_a_secure_secret
PORT=5001
# The URL of the frontend app (used for CORS). On Render it will be like https://your-app.onrender.com
CLIENT_URL=https://your-frontend-url.onrender.com
